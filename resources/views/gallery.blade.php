@extends('layouts.app')
@section('title','Galerie — A.S ZINGA')
@section('content')<section class="mx-auto max-w-7xl px-4 py-16 lg:px-8"><p class="font-black uppercase tracking-widest text-orange-600">En images</p><h1 class="mt-2 text-4xl font-black">Galerie</h1><div class="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">@forelse($items as $item)<figure class="overflow-hidden rounded-2xl bg-white shadow-sm"><img src="{{ asset('storage/'.$item->image_path) }}" alt="{{ $item->title ?: 'A.S ZINGA' }}" class="aspect-[4/3] w-full object-cover"><figcaption class="p-4">@if($item->title)<strong>{{ $item->title }}</strong>@endif @if($item->caption)<p class="mt-1 text-sm text-zinc-600">{{ $item->caption }}</p>@endif</figcaption></figure>@empty<div class="sm:col-span-2 lg:col-span-3 rounded-2xl border border-dashed border-zinc-300 p-10 text-center text-zinc-500">Les photos officielles seront publiées ici depuis l’administration.</div>@endforelse</div><div class="mt-6">{{ $items->links() }}</div></section>@endsection
