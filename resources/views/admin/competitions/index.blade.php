@extends('layouts.admin')
@section('title','Compétitions')
@section('content')
<div class="flex flex-wrap items-center justify-between gap-4"><div><p class="text-sm font-black uppercase tracking-widest text-orange-600">Sport</p><h1 class="text-3xl font-black">Compétitions & classements</h1></div><a href="{{ route('admin.competitions.create') }}" class="rounded-xl bg-orange-500 px-5 py-3 font-black text-white">Nouvelle compétition</a></div>
<div class="mt-7 space-y-4">@forelse($competitions as $competition)<div class="flex flex-col gap-4 rounded-2xl bg-white p-5 shadow-sm sm:flex-row sm:items-center sm:justify-between"><div><div class="flex flex-wrap items-center gap-2"><h2 class="text-xl font-black">{{ $competition->name }}</h2><span class="rounded-full px-2.5 py-1 text-xs font-bold {{ $competition->is_active ? 'bg-emerald-100 text-emerald-700' : 'bg-zinc-100 text-zinc-500' }}">{{ $competition->is_active ? 'Publiée' : 'Masquée' }}</span></div><p class="mt-1 text-sm text-zinc-500">{{ $competition->season ?: 'Saison non renseignée' }} · {{ $competition->standings_count }} équipe{{ $competition->standings_count > 1 ? 's' : '' }}</p></div><div class="flex gap-2"><a href="{{ route('admin.competitions.edit',$competition) }}" class="rounded-lg bg-zinc-950 px-4 py-2 text-sm font-bold text-white">Gérer</a><form method="POST" action="{{ route('admin.competitions.destroy',$competition) }}" onsubmit="return confirm('Supprimer cette compétition et son classement ?')">@csrf @method('DELETE')<button class="rounded-lg bg-red-50 px-4 py-2 text-sm font-bold text-red-700">Supprimer</button></form></div></div>@empty<div class="rounded-2xl border border-dashed border-zinc-300 bg-white p-10 text-center text-zinc-500">Aucune compétition enregistrée.</div>@endforelse</div>
@endsection
