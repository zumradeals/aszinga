@extends('layouts.admin')
@section('title','Partenaires')
@section('content')<div class="flex items-center justify-between gap-4"><div><p class="text-sm font-bold text-orange-600">Club</p><h1 class="text-3xl font-black">Partenaires</h1></div><a href="{{ route('admin.partners.create') }}" class="rounded-xl bg-orange-500 px-4 py-3 font-bold text-white">+ Partenaire</a></div><div class="mt-6 overflow-hidden rounded-2xl bg-white shadow-sm">@forelse($partners as $partner)<div class="flex items-center justify-between gap-4 border-b p-4 last:border-0"><div><strong>{{ $partner->name }}</strong><p class="text-sm text-zinc-500">{{ $partner->is_active?'Visible':'Masqué' }}</p></div><a href="{{ route('admin.partners.edit',$partner) }}" class="text-sm font-bold text-orange-600">Modifier</a></div>@empty<div class="p-8 text-zinc-500">Aucun partenaire enregistré.</div>@endforelse</div><div class="mt-5">{{ $partners->links() }}</div>@endsection
