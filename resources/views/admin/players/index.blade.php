@extends('layouts.admin')
@section('title','Joueurs')
@section('content')<div class="flex items-center justify-between gap-4"><div><p class="text-sm font-bold text-orange-600">Effectif</p><h1 class="text-3xl font-black">Joueurs</h1></div><a href="{{ route('admin.players.create') }}" class="rounded-xl bg-orange-500 px-4 py-3 font-bold text-white">+ Joueur</a></div><div class="mt-6 overflow-hidden rounded-2xl bg-white shadow-sm">@forelse($players as $player)<div class="flex items-center justify-between gap-4 border-b border-zinc-100 p-4 last:border-0"><div class="flex min-w-0 items-center gap-4"><div class="grid size-11 shrink-0 place-items-center rounded-xl bg-zinc-950 font-black text-white">{{ $player->shirt_number ?? '—' }}</div><div class="min-w-0"><strong class="block truncate">{{ $player->display_name ?: $player->first_name.' '.$player->last_name }}</strong><span class="text-sm text-zinc-500">{{ $player->position }} · {{ $player->is_active ? 'Actif' : 'Inactif' }}</span></div></div><a href="{{ route('admin.players.edit',$player) }}" class="shrink-0 text-sm font-bold text-orange-600">Modifier</a></div>@empty<div class="p-8 text-center text-zinc-500">Aucun joueur enregistré.</div>@endforelse</div><div class="mt-5">{{ $players->links() }}</div>@endsection