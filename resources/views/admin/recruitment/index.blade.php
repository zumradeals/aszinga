@extends('layouts.admin')
@section('title','Candidatures')
@section('content')<div><p class="text-sm font-bold text-orange-600">Détection</p><h1 class="text-3xl font-black">Candidatures</h1></div><div class="mt-6 overflow-hidden rounded-2xl bg-white shadow-sm">@forelse($applications as $application)<div class="flex items-center justify-between gap-4 border-b p-4 last:border-0"><div class="min-w-0"><strong class="block truncate">{{ $application->first_name }} {{ $application->last_name }}</strong><p class="text-sm text-zinc-500">{{ $application->position ?: 'Poste non précisé' }} · {{ $application->phone }} · {{ $application->status }}</p></div><a href="{{ route('admin.recruitment.edit',$application) }}" class="shrink-0 text-sm font-bold text-orange-600">Ouvrir</a></div>@empty<div class="p-8 text-zinc-500">Aucune candidature reçue.</div>@endforelse</div><div class="mt-5">{{ $applications->links() }}</div>@endsection
