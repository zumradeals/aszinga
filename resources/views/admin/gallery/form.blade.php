@extends('layouts.admin')
@section('title',$item->exists?'Modifier une photo':'Ajouter une photo')
@section('content')<h1 class="text-3xl font-black">{{ $item->exists?'Modifier la photo':'Ajouter une photo' }}</h1><form method="POST" enctype="multipart/form-data" action="{{ $item->exists?route('admin.gallery.update',$item):route('admin.gallery.store') }}" class="mt-6 max-w-3xl rounded-2xl bg-white p-5 sm:p-7">@csrf @if($item->exists) @method('PUT') @endif @if($errors->any())<div class="mb-5 rounded-xl bg-red-50 p-4 text-red-700">{{ $errors->first() }}</div>@endif<div class="grid gap-5 sm:grid-cols-2"><label class="text-sm font-bold">Titre<input name="title" value="{{ old('title',$item->title) }}" class="mt-2 w-full rounded-xl border border-zinc-300 px-4 py-3 font-normal"></label><label class="text-sm font-bold">Date de la photo<input type="date" name="taken_at" value="{{ old('taken_at',optional($item->taken_at)->format('Y-m-d')) }}" class="mt-2 w-full rounded-xl border border-zinc-300 px-4 py-3 font-normal"></label><label class="text-sm font-bold sm:col-span-2">Image<input type="file" accept="image/*" name="image" {{ $item->exists?'':'required' }} class="mt-2 block w-full rounded-xl border border-zinc-300 p-3 font-normal"></label></div><label class="mt-5 block text-sm font-bold">Légende<textarea name="caption" rows="4" class="mt-2 w-full rounded-xl border border-zinc-300 px-4 py-3 font-normal">{{ old('caption',$item->caption) }}</textarea></label><label class="mt-5 flex items-center gap-2 text-sm font-bold"><input type="hidden" name="is_published" value="0"><input type="checkbox" name="is_published" value="1" @checked(old('is_published',$item->exists?$item->is_published:true))> Publier sur le site</label><div class="mt-6 flex gap-3"><button class="rounded-xl bg-orange-500 px-5 py-3 font-black text-white">Enregistrer</button><a href="{{ route('admin.gallery.index') }}" class="rounded-xl border px-5 py-3 font-bold">Annuler</a></div></form>@endsection
