@extends('layouts.admin')
@section('title','Galerie')
@section('content')<div class="flex items-center justify-between gap-4"><div><p class="text-sm font-bold text-orange-600">Médias</p><h1 class="text-3xl font-black">Galerie</h1></div><a href="{{ route('admin.gallery.create') }}" class="rounded-xl bg-orange-500 px-4 py-3 font-bold text-white">+ Photo</a></div><div class="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">@forelse($items as $item)<article class="overflow-hidden rounded-2xl bg-white shadow-sm">@if($item->image_path)<img src="{{ asset('storage/'.$item->image_path) }}" alt="" class="aspect-video w-full object-cover">@endif<div class="p-4"><strong>{{ $item->title ?: 'Photo sans titre' }}</strong><p class="mt-1 text-sm text-zinc-500">{{ $item->is_published?'Publiée':'Masquée' }}</p><div class="mt-3 flex gap-4"><a href="{{ route('admin.gallery.edit',$item) }}" class="text-sm font-bold text-orange-600">Modifier</a><form method="POST" action="{{ route('admin.gallery.destroy',$item) }}" onsubmit="return confirm('Supprimer cette photo ?')">@csrf @method('DELETE')<button class="text-sm font-bold text-red-600">Supprimer</button></form></div></div></article>@empty<div class="rounded-2xl bg-white p-8 text-zinc-500">Aucune photo.</div>@endforelse</div><div class="mt-5">{{ $items->links() }}</div>@endsection
