@extends('layouts.app')
@section('title','Partenaires — A.S ZINGA')
@section('content')<section class="mx-auto max-w-7xl px-4 py-16 lg:px-8"><p class="font-black uppercase tracking-widest text-orange-600">Ils nous accompagnent</p><h1 class="mt-2 text-4xl font-black">Partenaires</h1><div class="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">@forelse($partners as $partner)<article class="flex min-h-44 flex-col items-center justify-center rounded-2xl bg-white p-6 text-center shadow-sm">@if($partner->logo_path)<img src="{{ asset('storage/'.$partner->logo_path) }}" alt="{{ $partner->name }}" class="mb-4 max-h-20 max-w-40 object-contain">@endif<strong>{{ $partner->name }}</strong>@if($partner->website_url)<a href="{{ $partner->website_url }}" target="_blank" rel="noopener noreferrer" class="mt-2 text-sm font-bold text-orange-600">Visiter le site</a>@endif</article>@empty<div class="sm:col-span-2 lg:col-span-4 rounded-2xl border border-dashed border-zinc-300 p-10 text-center text-zinc-500">Les partenaires officiels apparaîtront ici après validation du club.</div>@endforelse</div></section>@endsection
